# Model Files

The trained model files are not included in this repository because model binaries can be large.

If you have permission to publish a trained model, place it in this folder and document:
- model name
- framework/version
- input size
- classes
- how the model was trained

Do not upload confidential, proprietary, or restricted datasets/models.
